const searchContainer = document.querySelector('.search-container');
const btnSearch = document.querySelector('.search-icon');

btnSearch.addEventListener('click',()=>{
    searchContainer.classList.toogle('active')

});